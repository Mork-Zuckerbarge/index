<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BBQ Index Checker</title>
    <style>
        body {
            font-family: 'Creepster', cursive;
            background-color: #f8f9fa;
            margin: 0;
            padding: 20px;
            color: #333;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            background: #ffffff;
            border: 1px solid #ddd;
            border-radius: 8px;
            padding: 20px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        h1 {
            text-align: center;
            color: #6c757d;
        }

        form {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        input[type="text"] {
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 4px;
            font-size: 16px;
        }

        button {
            padding: 10px;
            font-size: 16px;
            color: #fff;
            background-color: #28a745;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        button:hover {
            background-color: #218838;
        }

        .result {
            margin-top: 20px;
            padding: 15px;
            background: #e9ecef;
            border-radius: 4px;
        }

        .error {
            color: red;
        }
    </style>
    <link href="https://fonts.googleapis.com/css2?family=Creepster&display=swap" rel="stylesheet">
</head>
<body>
    <div class="container">
        <h1>BBQ Index Checker</h1>
        <p>Enter the ticker of a Solana token to see how it fares in the BBQ Index.</p>

        <form id="bbqForm">
            <input type="text" id="tokenTicker" placeholder="Enter token ticker (e.g., SOL, SRM)" required>
            <button type="submit">Check BBQ Index</button>
        </form>

        <div id="result" class="result" style="display: none;"></div>
    </div>

    <script>
        document.getElementById('bbqForm').addEventListener('submit', async function(e) {
            e.preventDefault();

            const resultDiv = document.getElementById('result');
            const ticker = document.getElementById('tokenTicker').value.trim();

            if (!ticker) {
                resultDiv.style.display = 'block';
                resultDiv.innerHTML = '<span class="error">Please enter a token ticker.</span>';
                return;
            }

            resultDiv.style.display = 'block';
            resultDiv.textContent = 'Fetching BBQ Index for ' + ticker + '...';

            try {
                // Replace this URL with your BBQ Index API endpoint
                const response = await fetch(`https://api.example.com/bbq-index?ticker=${encodeURIComponent(ticker)}`);

                if (!response.ok) {
                    throw new Error('Unable to fetch data. Please try again later.');
                }

                const data = await response.json();

                if (data.success) {
                    resultDiv.innerHTML = `
                        <h3>${ticker.toUpperCase()} BBQ Index Results</h3>
                        <p><strong>BBQ Score:</strong> ${data.score}</p>
                        <p><strong>Description:</strong> ${data.description}</p>
                    `;
                } else {
                    resultDiv.innerHTML = `<span class="error">${data.message || 'No results found for this ticker.'}</span>`;
                }
            } catch (error) {
                console.error('Error:', error);
                resultDiv.innerHTML = `<span class="error">Error fetching data: ${error.message}</span>`;
            }
        });
    </script>
</body>
</html>
